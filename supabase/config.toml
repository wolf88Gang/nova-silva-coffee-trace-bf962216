# Configuración para Edge Functions
# ensure-demo-user: debe ser invocable sin sesión (usuario anónimo en /demo)
[functions.ensure-demo-user]
verify_jwt = false

[functions.rls-smoke-test]
verify_jwt = true
